import { 
  NotificationType,
  OrderStatus,
  PlanTier,
  Platform,
  ProviderStatus,
  RefillPolicy,
  ReferralStatus,
  TicketCategory,
  TicketStatus,
  TransactionStatus,
  TransactionType,
  UserRole,
 } from "../backend";
import type { backendInterface } from "../backend";
import { Principal } from "@icp-sdk/core/principal";

const mockPrincipal = Principal.fromText("aaaaa-aa");
const now = BigInt(Date.now()) * BigInt(1_000_000);

const mockUser = {
  id: mockPrincipal,
  username: "john_doe",
  email: "john@example.com",
  createdAt: now,
  role: UserRole.user,
  avatar: undefined,
};

const mockServices = [
  {
    id: BigInt(1), name: "Instagram Followers", platform: Platform.instagram,
    pricePerThousand: BigInt(50), minQuantity: BigInt(100), maxQuantity: BigInt(100000),
    deliveryTimeHours: BigInt(24), hasRefill: true, qualityRating: BigInt(5),
    reliabilityScore: BigInt(97), isActive: true, category: "followers",
    basicPrice: BigInt(50), recommendedPrice: BigInt(63), premiumPrice: BigInt(94),
    baseCost: BigInt(30), successRate: BigInt(97), retentionScore: BigInt(85),
    refillPolicy: RefillPolicy.guaranteed, currencyPrices: [] as Array<[string, number]>,
    urgencySignal: "High demand service", providerId: undefined,
  },
  {
    id: BigInt(2), name: "Instagram Likes", platform: Platform.instagram,
    pricePerThousand: BigInt(10), minQuantity: BigInt(50), maxQuantity: BigInt(50000),
    deliveryTimeHours: BigInt(1), hasRefill: false, qualityRating: BigInt(4),
    reliabilityScore: BigInt(99), isActive: true, category: "likes",
    basicPrice: BigInt(10), recommendedPrice: BigInt(13), premiumPrice: BigInt(19),
    baseCost: BigInt(6), successRate: BigInt(99), retentionScore: BigInt(75),
    refillPolicy: RefillPolicy.limited, currencyPrices: [] as Array<[string, number]>,
    urgencySignal: "Limited slots today", providerId: undefined,
  },
  {
    id: BigInt(4), name: "YouTube Subscribers", platform: Platform.youtube,
    pricePerThousand: BigInt(300), minQuantity: BigInt(100), maxQuantity: BigInt(50000),
    deliveryTimeHours: BigInt(48), hasRefill: true, qualityRating: BigInt(5),
    reliabilityScore: BigInt(95), isActive: true, category: "subscribers",
    basicPrice: BigInt(300), recommendedPrice: BigInt(375), premiumPrice: BigInt(563),
    baseCost: BigInt(180), successRate: BigInt(95), retentionScore: BigInt(88),
    refillPolicy: RefillPolicy.guaranteed, currencyPrices: [] as Array<[string, number]>,
    urgencySignal: "High demand service", providerId: undefined,
  },
  {
    id: BigInt(7), name: "TikTok Followers", platform: Platform.tiktok,
    pricePerThousand: BigInt(40), minQuantity: BigInt(100), maxQuantity: BigInt(100000),
    deliveryTimeHours: BigInt(12), hasRefill: true, qualityRating: BigInt(4),
    reliabilityScore: BigInt(96), isActive: true, category: "followers",
    basicPrice: BigInt(40), recommendedPrice: BigInt(50), premiumPrice: BigInt(75),
    baseCost: BigInt(24), successRate: BigInt(96), retentionScore: BigInt(80),
    refillPolicy: RefillPolicy.limited, currencyPrices: [] as Array<[string, number]>,
    urgencySignal: "", providerId: undefined,
  },
  {
    id: BigInt(11), name: "Twitter Followers", platform: Platform.twitter,
    pricePerThousand: BigInt(80), minQuantity: BigInt(100), maxQuantity: BigInt(50000),
    deliveryTimeHours: BigInt(24), hasRefill: true, qualityRating: BigInt(4),
    reliabilityScore: BigInt(95), isActive: true, category: "followers",
    basicPrice: BigInt(80), recommendedPrice: BigInt(100), premiumPrice: BigInt(150),
    baseCost: BigInt(48), successRate: BigInt(90), retentionScore: BigInt(78),
    refillPolicy: RefillPolicy.none, currencyPrices: [] as Array<[string, number]>,
    urgencySignal: "", providerId: undefined,
  },
];

const mockOrders = [
  {
    id: BigInt(1),
    userId: mockPrincipal,
    serviceId: BigInt(1),
    link: "https://instagram.com/johndoe",
    quantity: BigInt(1000),
    totalPrice: BigInt(50),
    status: OrderStatus.completed,
    createdAt: now - BigInt(86400_000_000_000),
    estimatedDelivery: now,
    completedAt: now,
  },
  {
    id: BigInt(2),
    userId: mockPrincipal,
    serviceId: BigInt(4),
    link: "https://youtube.com/@johndoe",
    quantity: BigInt(500),
    totalPrice: BigInt(150),
    status: OrderStatus.processing,
    createdAt: now - BigInt(3600_000_000_000),
    estimatedDelivery: now + BigInt(86400_000_000_000),
    completedAt: undefined,
  },
];

const mockTransactions = [
  {
    id: BigInt(1),
    userId: mockPrincipal,
    amount: BigInt(1000),
    txType: TransactionType.credit,
    status: TransactionStatus.completed,
    description: "Wallet top-up",
    createdAt: now - BigInt(172800_000_000_000),
  },
  {
    id: BigInt(2),
    userId: mockPrincipal,
    amount: BigInt(50),
    txType: TransactionType.debit,
    status: TransactionStatus.completed,
    description: "Order #1 - Instagram Followers",
    createdAt: now - BigInt(86400_000_000_000),
  },
];

const mockNotifications = [
  {
    id: BigInt(1),
    userId: mockPrincipal,
    message: "Your order #2 is now processing.",
    notifType: NotificationType.order_update,
    isRead: false,
    createdAt: now - BigInt(3600_000_000_000),
  },
];

const mockTickets = [
  {
    id: BigInt(1),
    userId: mockPrincipal,
    subject: "Order not delivered",
    category: TicketCategory.order,
    status: TicketStatus.open,
    createdAt: now - BigInt(86400_000_000_000),
    lastUpdatedAt: now,
  },
];

const mockPlans = [
  {
    id: BigInt(0),
    tier: PlanTier.free,
    name: "Free",
    priceMonthly: BigInt(0),
    discountPercent: BigInt(0),
    features: ["Basic services", "Standard delivery"],
  },
  {
    id: BigInt(1),
    tier: PlanTier.pro,
    name: "Pro",
    priceMonthly: BigInt(9900),
    discountPercent: BigInt(15),
    features: ["All services", "15% discount", "Priority support"],
  },
  {
    id: BigInt(2),
    tier: PlanTier.premium,
    name: "Premium",
    priceMonthly: BigInt(19900),
    discountPercent: BigInt(25),
    features: ["All services", "25% discount", "Priority delivery", "Dedicated support"],
  },
];

export const mockBackend: backendInterface = {
  registerUser: async () => mockUser,
  getMyProfile: async () => mockUser,
  updateMyProfile: async (username, email) => ({ ...mockUser, username, email }),
  getMyBalance: async () => BigInt(950),
  getMyStats: async () => ({
    totalOrders: BigInt(12),
    completedOrders: BigInt(10),
    activeOrders: BigInt(2),
    totalSpend: BigInt(350),
  }),
  getMyOrders: async () => mockOrders,
  getOrderById: async () => mockOrders[0],
  placeOrder: async (req) => ({
    id: BigInt(3),
    userId: mockPrincipal,
    serviceId: req.serviceId,
    link: req.link,
    quantity: req.quantity,
    totalPrice: BigInt(100),
    status: OrderStatus.pending,
    createdAt: now,
    estimatedDelivery: now + BigInt(86400_000_000_000),
    completedAt: undefined,
  }),
  cancelOrder: async () => undefined,
  listServices: async () => mockServices,
  getServicesByPlatform: async () => mockServices,
  getServicesSorted: async () => mockServices,
  searchServices: async () => mockServices,
  getMyTransactions: async () => mockTransactions,
  getMyNotifications: async () => mockNotifications,
  getUnreadNotificationCount: async () => BigInt(1),
  markNotificationRead: async () => undefined,
  getMyTickets: async () => mockTickets,
  getTicketDetail: async () => ({
    ticket: mockTickets[0],
    messages: [
      {
        id: BigInt(1),
        ticketId: BigInt(1),
        authorId: mockPrincipal,
        message: "Hello, my order hasn't been delivered yet.",
        isAdminReply: false,
        createdAt: now - BigInt(86400_000_000_000),
      },
    ],
  }),
  createTicket: async (subject, category) => ({
    id: BigInt(2),
    userId: mockPrincipal,
    subject,
    category,
    status: TicketStatus.open,
    createdAt: now,
    lastUpdatedAt: now,
  }),
  replyToTicket: async (ticketId, message) => ({
    id: BigInt(2),
    ticketId,
    authorId: mockPrincipal,
    message,
    isAdminReply: false,
    createdAt: now,
  }),
  closeMyTicket: async () => undefined,
  getSubscriptionPlans: async () => mockPlans,
  getMySubscription: async () => null,
  subscribeToPlan: async (planId) => ({
    userId: mockPrincipal,
    planId,
    isActive: true,
    autoRenew: true,
    startDate: now,
    endDate: now + BigInt(30 * 86400_000_000_000),
  }),
  cancelMySubscription: async () => undefined,
  generateApiKey: async (name) => ({
    id: BigInt(1),
    userId: mockPrincipal,
    name,
    maskedKey: "sk-****-****-abcd",
    isActive: true,
    usageCount: BigInt(0),
    createdAt: now,
    lastUsed: undefined,
  }),
  listMyApiKeys: async () => [],
  revokeApiKey: async () => undefined,
  adminListUsers: async () => [mockUser],
  adminListOrders: async () => mockOrders,
  adminListTickets: async () => mockTickets,
  adminAddFunds: async (userId, amount, description) => ({
    id: BigInt(10),
    userId,
    amount,
    txType: TransactionType.credit,
    status: TransactionStatus.completed,
    description,
    createdAt: now,
  }),
  adminAddService: async (name, platform) => ({
    id: BigInt(16), name, platform,
    pricePerThousand: BigInt(50), minQuantity: BigInt(100), maxQuantity: BigInt(10000),
    deliveryTimeHours: BigInt(24), hasRefill: false, qualityRating: BigInt(4),
    reliabilityScore: BigInt(95), isActive: true, category: "general",
    basicPrice: BigInt(50), recommendedPrice: BigInt(63), premiumPrice: BigInt(94),
    baseCost: BigInt(30), successRate: BigInt(90), retentionScore: BigInt(80),
    refillPolicy: RefillPolicy.none, currencyPrices: [] as Array<[string, number]>,
    urgencySignal: "", providerId: undefined,
  }),
  adminReplyToTicket: async (ticketId, message) => ({
    id: BigInt(3),
    ticketId,
    authorId: mockPrincipal,
    message,
    isAdminReply: true,
    createdAt: now,
  }),
  adminSetUserRole: async () => undefined,
  adminUpdateOrderStatus: async () => undefined,
  adminUpdateService: async () => undefined,
  adminGetDashboardStats: async () => ({
    totalOrders: BigInt(0),
    subscriptionRevenue: { pro: BigInt(0), premium: BigInt(0), free: BigInt(0) },
    dailyRevenue: [],
    totalUsers: BigInt(0),
    totalRevenue: 0,
    topServices: [],
    totalActiveOrders: BigInt(0),
    profitMarginPercent: 0,
    newUsersThisMonth: BigInt(0),
    avgOrderValue: 0,
    dailyOrders: [],
  }),
  adminGetAllOrders: async () => [],
  adminUpdateOrderManual: async () => undefined,
  adminCancelOrder: async () => undefined,
  adminGetAllUsers: async () => [],
  adminBanUser: async () => undefined,
  adminAdjustUserWallet: async () => undefined,
  adminGetServiceStats: async () => [],
  adminUpdateServiceVisibility: async () => undefined,
  adminBatchUpdateServicePrices: async () => undefined,
  adminAddProvider: async (name, apiUrl) => ({
    id: BigInt(99),
    name,
    apiUrl,
    apiKeyMasked: "****",
    commissionPercent: 15,
    priority: BigInt(1),
    status: ProviderStatus.active,
    successRate: 99,
    avgResponseMs: BigInt(400),
    failedLast24h: BigInt(0),
  }),
  adminUpdateProvider: async () => undefined,
  adminGetProviders: async () => [],
  adminGetLogs: async () => [],
  adminGetFraudAlerts: async () => [],
  adminResolveFraudAlert: async () => undefined,
  adminGetConfig: async () => ({
    referralsEnabled: true,
    globalMarginPercent: 22,
    maxOrdersPerUserPerDay: BigInt(50),
    autoRefundHours: BigInt(72),
    fraudDuplicateLinkThreshold: BigInt(3),
    subscriptionsEnabled: true,
    fraudVelocityLimit: BigInt(10),
    minUserAgeDays: BigInt(0),
    maxConcurrentOrdersPerLink: BigInt(3),
  }),
  adminUpdateConfig: async () => undefined,
  // New engagement/pricing/bundle methods
  adminAddBundle: async () => ({ id: BigInt(1), name: "", description: "", services: [], originalPrice: BigInt(0), discountedPrice: BigInt(0), estimatedDeliveryHours: BigInt(24), hasPremiumUpgrade: false, badge: "", imageEmoji: "", isActive: true }),
  adminUpdateBundle: async () => true,
  getBundle: async () => null,
  listBundles: async () => [],
  adminAddCoupon: async () => ({ id: BigInt(1), code: "", discountPercentage: 0, maxUsageCount: BigInt(100), usageCount: BigInt(0), expiryDate: BigInt(0), applicableServices: [], isActive: true, createdAt: BigInt(0) }),
  listMyCoupons: async () => [],
  validateCoupon: async () => null,
  adminAddCurrency: async () => ({ code: "INR", symbol: "\u20B9", flag: "\uD83C\uDDEE\uD83C\uDDF3", displayName: "Indian Rupee", exchangeRate: 1, isActive: true }),
  listCurrencies: async () => [],
  adminUpdateExchangeRate: async () => true,
  convertPrice: async () => null,
  claimDailyLoginReward: async () => BigInt(10),
  spinWheelReward: async () => BigInt(50),
  getMyEngagementHistory: async () => [],
  getMyLevel: async () => null,
  listUserLevels: async () => [],
  getMyReferrals: async () => [],
  createReferral: async () => ({ id: BigInt(1), referrerId: mockPrincipal, referredUserId: mockPrincipal, status: ReferralStatus.pending, earnings: BigInt(0), commissionPercent: 5, createdAt: BigInt(0) }),
  adminGetPricingRules: async () => [],
  adminAddPricingRule: async () => ({ id: BigInt(1), targetServices: [], basicMargin: 1, recommendedMargin: 1.25, premiumMargin: 1.875, currencyOverrides: [], isActive: true, createdAt: BigInt(0) }),
  calculateTierPrice: async () => null,
  listServiceTiers: async () => [],
  adminUpdateTier: async () => true,
  getServicePricing: async () => [0, 0, 0] as [number, number, number],
  getServiceReviews: async () => [],
  getAllApprovedReviews: async () => [],
  submitReview: async () => ({ id: BigInt(1), serviceId: BigInt(1), userId: mockPrincipal, rating: BigInt(5), comment: "", adminApproved: false, displayOrder: BigInt(0), createdAt: BigInt(0) }),
  adminApproveReview: async () => undefined,
  adminGetWalletRewards: async () => [],
  adminUpdateWalletReward: async () => undefined,
};
